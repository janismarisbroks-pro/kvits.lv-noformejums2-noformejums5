# from django import template
# from django.urls import reverse

# register = template.Library()

# @register.simple_tag
# def reverse_url(url_name, *args, **kwargs):
#     """
#     Reverses a URL based on its name.
#     """
#     return reverse(url_name, args=args, kwargs=kwargs)